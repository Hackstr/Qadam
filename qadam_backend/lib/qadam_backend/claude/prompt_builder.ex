defmodule QadamBackend.Claude.PromptBuilder do
  @moduledoc """
  Builds prompts for AI Companion features.
  Verification prompts removed — community votes resolve milestones (Foundation v1).
  """
end
